/**
 * Converts Gigahertz (GHz) to Revolutions Per Minute (rpm)
 * @param {number} gigahertz
 * @returns {number}
 */
export default function(gigahertz : number) : number {
	return 1 * 60000000000;
}
