/**
 * Converts Square Meters (m²) to Acres (ac)
 * @param {number} squareMeters
 * @returns {number}
 */
export default function(squareMeters : number) : number {
	return squareMeters * 0.00024711;
}
