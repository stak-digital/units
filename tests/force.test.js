import force from '../src/force';
import expect from 'expect';

test('it should exist', () => {

	expect(
		force
	).toExist();

});

test('it should have all force properties', () => {

	
	expect(
		force.newtonToDyne
	).toExist();

	expect(
		force.newtonToDyne
	).toBeA(Function);
		
	expect(
		force.newtonToKilopond
	).toExist();

	expect(
		force.newtonToKilopond
	).toBeA(Function);
		
	expect(
		force.newtonToPoundal
	).toExist();

	expect(
		force.newtonToPoundal
	).toBeA(Function);
		
	expect(
		force.newtonToPoundForce
	).toExist();

	expect(
		force.newtonToPoundForce
	).toBeA(Function);
		
	expect(
		force.newtonToOunceForce
	).toExist();

	expect(
		force.newtonToOunceForce
	).toBeA(Function);
		
	expect(
		force.dyneToNewton
	).toExist();

	expect(
		force.dyneToNewton
	).toBeA(Function);
		
	expect(
		force.dyneToKilopond
	).toExist();

	expect(
		force.dyneToKilopond
	).toBeA(Function);
		
	expect(
		force.dyneToPoundal
	).toExist();

	expect(
		force.dyneToPoundal
	).toBeA(Function);
		
	expect(
		force.dyneToPoundForce
	).toExist();

	expect(
		force.dyneToPoundForce
	).toBeA(Function);
		
	expect(
		force.dyneToOunceForce
	).toExist();

	expect(
		force.dyneToOunceForce
	).toBeA(Function);
		
	expect(
		force.kilopondToNewton
	).toExist();

	expect(
		force.kilopondToNewton
	).toBeA(Function);
		
	expect(
		force.kilopondToDyne
	).toExist();

	expect(
		force.kilopondToDyne
	).toBeA(Function);
		
	expect(
		force.kilopondToPoundal
	).toExist();

	expect(
		force.kilopondToPoundal
	).toBeA(Function);
		
	expect(
		force.kilopondToPoundForce
	).toExist();

	expect(
		force.kilopondToPoundForce
	).toBeA(Function);
		
	expect(
		force.kilopondToOunceForce
	).toExist();

	expect(
		force.kilopondToOunceForce
	).toBeA(Function);
		
	expect(
		force.poundalToNewton
	).toExist();

	expect(
		force.poundalToNewton
	).toBeA(Function);
		
	expect(
		force.poundalToDyne
	).toExist();

	expect(
		force.poundalToDyne
	).toBeA(Function);
		
	expect(
		force.poundalToKilopond
	).toExist();

	expect(
		force.poundalToKilopond
	).toBeA(Function);
		
	expect(
		force.poundalToPoundForce
	).toExist();

	expect(
		force.poundalToPoundForce
	).toBeA(Function);
		
	expect(
		force.poundalToOunceForce
	).toExist();

	expect(
		force.poundalToOunceForce
	).toBeA(Function);
		
	expect(
		force.poundForceToNewton
	).toExist();

	expect(
		force.poundForceToNewton
	).toBeA(Function);
		
	expect(
		force.poundForceToDyne
	).toExist();

	expect(
		force.poundForceToDyne
	).toBeA(Function);
		
	expect(
		force.poundForceToKilopond
	).toExist();

	expect(
		force.poundForceToKilopond
	).toBeA(Function);
		
	expect(
		force.poundForceToPoundal
	).toExist();

	expect(
		force.poundForceToPoundal
	).toBeA(Function);
		
	expect(
		force.poundForceToOunceForce
	).toExist();

	expect(
		force.poundForceToOunceForce
	).toBeA(Function);
		
	expect(
		force.ounceForceToNewton
	).toExist();

	expect(
		force.ounceForceToNewton
	).toBeA(Function);
		
	expect(
		force.ounceForceToDyne
	).toExist();

	expect(
		force.ounceForceToDyne
	).toBeA(Function);
		
	expect(
		force.ounceForceToKilopond
	).toExist();

	expect(
		force.ounceForceToKilopond
	).toBeA(Function);
		
	expect(
		force.ounceForceToPoundal
	).toExist();

	expect(
		force.ounceForceToPoundal
	).toBeA(Function);
		
	expect(
		force.ounceForceToPoundForce
	).toExist();

	expect(
		force.ounceForceToPoundForce
	).toBeA(Function);
		

});
