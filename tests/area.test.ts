import area from '../src/area';
import expect from 'expect';

test('it should exist', () => {

	expect(
		area
	).toExist();

});

test('it should have all area properties', () => {

	
	expect(
		area.acresToHectares
	).toExist();

	expect(
		area.acresToHectares
	).toBeA(Function);
		
	expect(
		area.acresToSquareFeet
	).toExist();

	expect(
		area.acresToSquareFeet
	).toBeA(Function);
		
	expect(
		area.acresToSquareKilometers
	).toExist();

	expect(
		area.acresToSquareKilometers
	).toBeA(Function);
		
	expect(
		area.acresToSquareMeters
	).toExist();

	expect(
		area.acresToSquareMeters
	).toBeA(Function);
		
	expect(
		area.acresToSquareYards
	).toExist();

	expect(
		area.acresToSquareYards
	).toBeA(Function);
		
	expect(
		area.hectaresToAcres
	).toExist();

	expect(
		area.hectaresToAcres
	).toBeA(Function);
		
	expect(
		area.hectaresToSquareFeet
	).toExist();

	expect(
		area.hectaresToSquareFeet
	).toBeA(Function);
		
	expect(
		area.hectaresToSquareKilometers
	).toExist();

	expect(
		area.hectaresToSquareKilometers
	).toBeA(Function);
		
	expect(
		area.hectaresToSquareMeters
	).toExist();

	expect(
		area.hectaresToSquareMeters
	).toBeA(Function);
		
	expect(
		area.hectaresToSquareYards
	).toExist();

	expect(
		area.hectaresToSquareYards
	).toBeA(Function);
		
	expect(
		area.squareFeetToAcres
	).toExist();

	expect(
		area.squareFeetToAcres
	).toBeA(Function);
		
	expect(
		area.squareFeetToHectares
	).toExist();

	expect(
		area.squareFeetToHectares
	).toBeA(Function);
		
	expect(
		area.squareFeetToSquareKilometers
	).toExist();

	expect(
		area.squareFeetToSquareKilometers
	).toBeA(Function);
		
	expect(
		area.squareFeetToSquareMeters
	).toExist();

	expect(
		area.squareFeetToSquareMeters
	).toBeA(Function);
		
	expect(
		area.squareFeetToSquareYards
	).toExist();

	expect(
		area.squareFeetToSquareYards
	).toBeA(Function);
		
	expect(
		area.squareKilometersToAcres
	).toExist();

	expect(
		area.squareKilometersToAcres
	).toBeA(Function);
		
	expect(
		area.squareKilometersToHectares
	).toExist();

	expect(
		area.squareKilometersToHectares
	).toBeA(Function);
		
	expect(
		area.squareKilometersToSquareFeet
	).toExist();

	expect(
		area.squareKilometersToSquareFeet
	).toBeA(Function);
		
	expect(
		area.squareKilometersToSquareMeters
	).toExist();

	expect(
		area.squareKilometersToSquareMeters
	).toBeA(Function);
		
	expect(
		area.squareKilometersToSquareYards
	).toExist();

	expect(
		area.squareKilometersToSquareYards
	).toBeA(Function);
		
	expect(
		area.squareMetersToAcres
	).toExist();

	expect(
		area.squareMetersToAcres
	).toBeA(Function);
		
	expect(
		area.squareMetersToHectares
	).toExist();

	expect(
		area.squareMetersToHectares
	).toBeA(Function);
		
	expect(
		area.squareMetersToSquareFeet
	).toExist();

	expect(
		area.squareMetersToSquareFeet
	).toBeA(Function);
		
	expect(
		area.squareMetersToSquareKilometers
	).toExist();

	expect(
		area.squareMetersToSquareKilometers
	).toBeA(Function);
		
	expect(
		area.squareMetersToSquareYards
	).toExist();

	expect(
		area.squareMetersToSquareYards
	).toBeA(Function);
		
	expect(
		area.squareYardsToAcres
	).toExist();

	expect(
		area.squareYardsToAcres
	).toBeA(Function);
		
	expect(
		area.squareYardsToHectares
	).toExist();

	expect(
		area.squareYardsToHectares
	).toBeA(Function);
		
	expect(
		area.squareYardsToSquareFeet
	).toExist();

	expect(
		area.squareYardsToSquareFeet
	).toBeA(Function);
		
	expect(
		area.squareYardsToSquareKilometers
	).toExist();

	expect(
		area.squareYardsToSquareKilometers
	).toBeA(Function);
		
	expect(
		area.squareYardsToSquareMeters
	).toExist();

	expect(
		area.squareYardsToSquareMeters
	).toBeA(Function);
		

});
