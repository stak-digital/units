import time from '../src/time';
import expect from 'expect';

test('it should exist', () => {

	expect(
		time
	).toExist();

});

test('it should have all time properties', () => {

	
	expect(
		time.nanosecondsToMicroseconds
	).toExist();

	expect(
		time.nanosecondsToMicroseconds
	).toBeA(Function);
		
	expect(
		time.nanosecondsToMilliseconds
	).toExist();

	expect(
		time.nanosecondsToMilliseconds
	).toBeA(Function);
		
	expect(
		time.nanosecondsToSeconds
	).toExist();

	expect(
		time.nanosecondsToSeconds
	).toBeA(Function);
		
	expect(
		time.nanosecondsToMinutes
	).toExist();

	expect(
		time.nanosecondsToMinutes
	).toBeA(Function);
		
	expect(
		time.nanosecondsToHours
	).toExist();

	expect(
		time.nanosecondsToHours
	).toBeA(Function);
		
	expect(
		time.nanosecondsToDays
	).toExist();

	expect(
		time.nanosecondsToDays
	).toBeA(Function);
		
	expect(
		time.microsecondsToNanoseconds
	).toExist();

	expect(
		time.microsecondsToNanoseconds
	).toBeA(Function);
		
	expect(
		time.microsecondsToMilliseconds
	).toExist();

	expect(
		time.microsecondsToMilliseconds
	).toBeA(Function);
		
	expect(
		time.microsecondsToSeconds
	).toExist();

	expect(
		time.microsecondsToSeconds
	).toBeA(Function);
		
	expect(
		time.microsecondsToMinutes
	).toExist();

	expect(
		time.microsecondsToMinutes
	).toBeA(Function);
		
	expect(
		time.microsecondsToHours
	).toExist();

	expect(
		time.microsecondsToHours
	).toBeA(Function);
		
	expect(
		time.microsecondsToDays
	).toExist();

	expect(
		time.microsecondsToDays
	).toBeA(Function);
		
	expect(
		time.millisecondsToNanoseconds
	).toExist();

	expect(
		time.millisecondsToNanoseconds
	).toBeA(Function);
		
	expect(
		time.millisecondsToMicroseconds
	).toExist();

	expect(
		time.millisecondsToMicroseconds
	).toBeA(Function);
		
	expect(
		time.millisecondsToSeconds
	).toExist();

	expect(
		time.millisecondsToSeconds
	).toBeA(Function);
		
	expect(
		time.millisecondsToMinutes
	).toExist();

	expect(
		time.millisecondsToMinutes
	).toBeA(Function);
		
	expect(
		time.millisecondsToHours
	).toExist();

	expect(
		time.millisecondsToHours
	).toBeA(Function);
		
	expect(
		time.millisecondsToDays
	).toExist();

	expect(
		time.millisecondsToDays
	).toBeA(Function);
		
	expect(
		time.secondsToNanoseconds
	).toExist();

	expect(
		time.secondsToNanoseconds
	).toBeA(Function);
		
	expect(
		time.secondsToMicroseconds
	).toExist();

	expect(
		time.secondsToMicroseconds
	).toBeA(Function);
		
	expect(
		time.secondsToMilliseconds
	).toExist();

	expect(
		time.secondsToMilliseconds
	).toBeA(Function);
		
	expect(
		time.secondsToMinutes
	).toExist();

	expect(
		time.secondsToMinutes
	).toBeA(Function);
		
	expect(
		time.secondsToHours
	).toExist();

	expect(
		time.secondsToHours
	).toBeA(Function);
		
	expect(
		time.secondsToDays
	).toExist();

	expect(
		time.secondsToDays
	).toBeA(Function);
		
	expect(
		time.minutesToNanoseconds
	).toExist();

	expect(
		time.minutesToNanoseconds
	).toBeA(Function);
		
	expect(
		time.minutesToMicroseconds
	).toExist();

	expect(
		time.minutesToMicroseconds
	).toBeA(Function);
		
	expect(
		time.minutesToMilliseconds
	).toExist();

	expect(
		time.minutesToMilliseconds
	).toBeA(Function);
		
	expect(
		time.minutesToSeconds
	).toExist();

	expect(
		time.minutesToSeconds
	).toBeA(Function);
		
	expect(
		time.minutesToHours
	).toExist();

	expect(
		time.minutesToHours
	).toBeA(Function);
		
	expect(
		time.minutesToDays
	).toExist();

	expect(
		time.minutesToDays
	).toBeA(Function);
		
	expect(
		time.hoursToNanoseconds
	).toExist();

	expect(
		time.hoursToNanoseconds
	).toBeA(Function);
		
	expect(
		time.hoursToMicroseconds
	).toExist();

	expect(
		time.hoursToMicroseconds
	).toBeA(Function);
		
	expect(
		time.hoursToMilliseconds
	).toExist();

	expect(
		time.hoursToMilliseconds
	).toBeA(Function);
		
	expect(
		time.hoursToSeconds
	).toExist();

	expect(
		time.hoursToSeconds
	).toBeA(Function);
		
	expect(
		time.hoursToMinutes
	).toExist();

	expect(
		time.hoursToMinutes
	).toBeA(Function);
		
	expect(
		time.hoursToDays
	).toExist();

	expect(
		time.hoursToDays
	).toBeA(Function);
		
	expect(
		time.daysToNanoseconds
	).toExist();

	expect(
		time.daysToNanoseconds
	).toBeA(Function);
		
	expect(
		time.daysToMicroseconds
	).toExist();

	expect(
		time.daysToMicroseconds
	).toBeA(Function);
		
	expect(
		time.daysToMilliseconds
	).toExist();

	expect(
		time.daysToMilliseconds
	).toBeA(Function);
		
	expect(
		time.daysToSeconds
	).toExist();

	expect(
		time.daysToSeconds
	).toBeA(Function);
		
	expect(
		time.daysToMinutes
	).toExist();

	expect(
		time.daysToMinutes
	).toBeA(Function);
		
	expect(
		time.daysToHours
	).toExist();

	expect(
		time.daysToHours
	).toBeA(Function);
		

});
